# AI runs through the ai-proxy Edge Function (supabase/functions/ai-proxy) — the Anthropic key lives there
# as an Edge Function secret (ANTHROPIC_API_KEY), never in this app or its users' hands.

# Phase 0 cloud sync — see Project Settings > API in your Supabase project
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
